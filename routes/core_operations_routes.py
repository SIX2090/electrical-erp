"""Core operations route registration (delegated to routes.registry in recovery build)."""


def register_routes(app, deps):
    # Core routes are restored in routes.registry for this recovery build.
    return None
